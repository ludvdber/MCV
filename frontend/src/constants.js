/**
 * Constantes partagees dans le frontend.
 * Les constantes specifiques a un seul composant restent dans ce composant.
 */

/** Prefixe identifiant un dataset INDIVIDUAL (ex: IND_MY34_LS45.00). */
export const INDIVIDUAL_PREFIX = 'IND_';

/** Nombre de pas de temps dans un fichier MEAN (cycle diurne complet). */
export const MAX_TIMESTEPS = 48;
